# Admin section

These are files used to build course artifacts.

## Current Artifacts

Currently, we only have the postgres_docker_build folder.  That creates our free postgres container pre-loaded with database content.